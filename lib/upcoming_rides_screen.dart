import 'package:flutter/material.dart';

class UpcomingRidesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Your Upcoming Rides'),
      ),
      body: Center(
        child: Text('Upcoming Rides Screen'),
      ),
    );
  }
}
